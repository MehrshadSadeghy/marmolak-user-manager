api:
  debug: false
  host: 0.0.0.0
  port: 8080
  title: VPN User Manager
  version: 0.1.0

# DB name, user, and password come from POSTGRES_* env vars (docker compose .env).
postgres:
  host: postgres
  port: 5432
